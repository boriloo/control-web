{
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "copy-env": "node -e \"const fs=require('fs'); if(!fs.existsSync('./.env')) fs.copyFileSync('./.env.example', './.env')\"",
    "docker:up": "docker-compose up -d",
    "db:migrate": "npx dotenv-cli -- npx prisma migrate dev && npx prisma generate",
    "db:generate": "npx prisma generate",
    "dev:server": "cd server && npm run server",
    "dev:client": "cd client && npm run dev",
    "start:all": "npm run copy-env && npm run docker:up && npm run db:migrate && concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "@prisma/config": "^7.7.0",
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.7.0"
  }
}
